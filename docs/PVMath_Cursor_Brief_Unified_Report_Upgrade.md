# Cursor Brief — Rebuild the Unified PVMath Report

> **Status: DONE (29 Jun 2026)** — Shipped on `main` in commits `921f314`, `ef463c3`, `e05faf7`. Implementation: `pvmath_reports/` package. See `PVMath/STATUS.md` for handoff.

Paste this whole brief into Cursor as the task prompt.

---

## Context

The unified report at `/workflow/pvmath-report-pdf` (built by `pvmath_workflow/project_report.py::build_pvmath_report_pdf`) is visually weak — plain green-header tables only, no charts, no maps, no verdict cards. We already have three much better report generators elsewhere in the codebase. Your job is to rebuild the unified report by reusing/porting the good parts, not by inventing new design.

Reference reports (attached as PDFs for visual comparison, not in repo):
- `SiteIQ_Tehuacana_20260619-6.pdf` — good SiteIQ report, generated by **legacy Streamlit** `pages/siteiq.py::build_pdf` (~line 1243). Has: project info table, green "OVERALL VERDICT" box, scored "SITE SUITABILITY BREAKDOWN" table, "KEY DRIVERS" bullets, "PVMATH SCORE" panel, "KEY METRICS" table, a colored monthly solar irradiation bar chart (built around `pages/siteiq.py` line ~1635, `section_hdr("MONTHLY SOLAR IRRADIATION (kWh/m²)")`), "RECOMMENDED NEXT STEPS" list, and a "REFERENCE" verdict-scale table.
- `TopoIQ_31.855_-96.384_11m_report-4f6744e9.pdf` — good TerrainIQ report, generated by **`pvmath_terrain_report.py::generate_pdf_report(ctx)`** with context from `build_report_context(...)`, already called live from `api/routers/terrainiq.py::_build_topo_pdf` (lines 159-220). Includes a slope-map figure from `render_slope_map_png()` (matplotlib, satellite basemap + colorbar + north arrow + scale bar), a Project Summary table with reverse-geocoded location, Terrain Metrics table, Slope Direction table, Slope Distribution colored bins, dark-green Fixed Tilt / Tracker verdict cards, and threshold reference tables.
- `YieldIQ_Tehuacana-ad1fbb6c.pdf` — good YieldIQ report, generated by **legacy Streamlit** `pages/yieldiq.py::build_pdf` (line 306) + `make_monthly_chart()` (line 260, matplotlib grouped bar chart). Has: Solar Resource table, POA performance table, Losses Breakdown table, a 4-config "RESULTS — CONFIGURATION COMPARISON" table, and a colorful grouped "MONTHLY ENERGY PROFILE" bar chart (4 series × 12 months).
- `Parcel_1_PVMath_Report.pdf` — the current weak unified report. Plain tables only, no charts, no maps, no verdict cards.

Key fact about reuse vs. port:
- **TerrainIQ section: REUSE, don't rebuild.** It's already fully ported to the API (`pvmath_terrain_report.py` + `api/routers/terrainiq.py`). Call the same context-builder/renderer the standalone TerrainIQ report uses, and insert its flowables into the unified document instead of the current flat table in `project_report.py`.
- **SiteIQ section: PORT from Streamlit.** The rich version only exists in `pages/siteiq.py::build_pdf`. The current API-side SiteIQ PDF (`pvmath_gate/screening_pdf.py::build_screening_pdf`, served from `api/routers/reports.py`) is explicitly a "Minimal screening PDF" — it has none of the chart/verdict/breakdown elements. Port the scored breakdown table, verdict box, key drivers, monthly irradiation chart, and next-steps list logic from the Streamlit version into the unified report (or into a shared module both can call).
- **YieldIQ section: PORT from Streamlit — no API equivalent exists at all.** `api/routers/yieldiq.py` only has `/yieldiq/analyze`; there is no report endpoint. Port `pages/yieldiq.py::build_pdf` and `make_monthly_chart()` (both matplotlib-based) into the unified report.

## What to build

Rework `pvmath_workflow/project_report.py::build_pvmath_report_pdf` (served via `api/routers/workflow.py`'s `POST /workflow/pvmath-report-pdf`) so the PDF has this section order, in one document:

1. **Project Summary** — one consolidated summary block at the top: project name, country, land use/mounting, capacity, and a reverse-geocoded location line in the same style as the TopoIQ report's Project Summary table (same `resolve_location_label()` pattern already used in `pvmath_terrain_report.py`/`api/routers/terrainiq.py`). Do not repeat this summary information again at the start of each module section below — each section should only show its own metrics/charts.
2. **SiteIQ** — scored breakdown table, verdict box, key drivers, monthly solar irradiation chart, ported from `pages/siteiq.py::build_pdf`.
3. **TerrainIQ** — slope map figure, terrain metrics, slope direction/distribution tables, Fixed Tilt / Tracker verdict cards — reused from `pvmath_terrain_report.py` (the same code path as `api/routers/terrainiq.py::_build_topo_pdf`).
4. **YieldIQ** — solar resource table, losses breakdown, configuration comparison table, monthly energy profile chart — ported from `pages/yieldiq.py::build_pdf` + `make_monthly_chart()`.
5. **Disclaimers + Annex** — at the very end of the document only (current `SITEIQ_DISCLAIMER_BODY` footer content, plus any reference/threshold tables you want to keep as an annex). Don't scatter disclaimers mid-document.

### Remove this specific disclaimer line from the PDF

In the SiteIQ section, delete the rendering of the `terrain_note` line:

```python
# pvmath_workflow/project_report.py, lines ~159-161
if scr.get("terrain_note"):
    story.append(Spacer(1, 0.15 * cm))
    story.append(_lp(str(scr["terrain_note"]), st["muted"]))
```

This text ("Terrain slope is not assessed in site screening. Run TerrainIQ on your site boundary...") is defined in `pvmath_workflow/screen.py` (~line 60-61, `terrain_note: str = (...)`) and flows through `api/schemas/workflow.py` → `api/routers/workflow.py` (`terrain_note=result.terrain_note`). It's redundant in the unified PDF because TerrainIQ's full section follows immediately after. **Only remove it from this PDF's SiteIQ section** — leave `frontend/src/pages/OutputPage.tsx`'s on-screen rendering of `result.terrain_note` (around line 2442) untouched, since that's a separate, still-useful on-screen hint shown before the user runs TerrainIQ interactively.

### Match the React app's visual styling

Use the same accent green already used across the brand (`#1d9e52`, darker `#157a40`) — see `frontend/src/index.css` (`--accent: #1d9e52`) and `pvmath_brand.py`. For chart coloring specifically, mirror the palette/style already used in the TopoIQ slope map (`render_slope_map_png` in `pvmath_terrain_report.py`) and the YieldIQ monthly chart (`make_monthly_chart` in `pages/yieldiq.py`) rather than inventing a new chart style — the goal is visual consistency across all three module sections in one document, matching what's shown on the React app's results screens.

## Constraints

- Don't change anything in `pages/siteiq.py`, `pages/yieldiq.py`, or `pvmath_terrain_report.py` themselves beyond what's needed to extract/reuse their chart- and table-building logic (e.g., factor shared pieces into importable functions if they're currently tangled with Streamlit-only `st.*` calls).
- Don't touch `frontend/src/pages/OutputPage.tsx`'s on-screen `terrain_note` rendering.
- Don't change `build_layout_a3_pdf()` or `build_project_package_zip()` in `project_report.py` — out of scope.
- This only affects the unified `/workflow/pvmath-report-pdf` endpoint and its PDF output — not the standalone per-module report endpoints (`/reports/screening-pdf`, `/terrainiq/report-pdf`), which can keep existing behavior.
- Test against a real project end-to-end (generate the unified PDF) before considering this done — compare side-by-side with the three legacy PDFs attached to this brief.
